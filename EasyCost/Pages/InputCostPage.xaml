<Page
    x:Class="EasyCost.Pages.InputCostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyCost.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controls="using:EasyCost.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="0,20,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <RelativePanel Grid.Column="0" Margin=" 0,0,0,0" >
            <RelativePanel RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True">
                <TextBlock x:Name="lblTitle" RelativePanel.AlignLeftWithPanel="True" TextWrapping="Wrap" Text="지출 내역 입력" FontSize="20" FontWeight="Bold">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{ThemeResource SystemBaseHighColor}"/>
                    </TextBlock.Foreground>
                </TextBlock>
                <TextBlock x:Name="lblCategory" RelativePanel.AlignLeftWith="lblTitle" RelativePanel.Below="lblTitle" TextWrapping="Wrap" Text="지출 분류" FontSize="15" Foreground="Gray" Margin="0,20,0,0" />
                <ComboBox x:Name="cboCategory" RelativePanel.AlignVerticalCenterWith="lblCategory" RelativePanel.RightOf="lblCategory" Width="300" FontSize="15" Margin="30,20,0,0" BorderThickness="1" SelectionChanged="cboCategory_SelectionChanged" />
                <TextBlock x:Name="lblSubCategory" RelativePanel.AlignLeftWith="lblTitle" RelativePanel.Below="lblCategory" TextWrapping="Wrap" Text="세부 분류" FontSize="15" Foreground="Gray" Margin="0,20,0,0" />
                <ComboBox x:Name="cboSubCategory" RelativePanel.AlignVerticalCenterWith="lblSubCategory" RelativePanel.RightOf="lblSubCategory" Width="300" FontSize="15" Margin="30,20,0,0" BorderThickness="1" />
                <TextBlock x:Name="lblDetail" Text="지출 내역" RelativePanel.AlignLeftWith="lblTitle" RelativePanel.Below="lblSubCategory" FontSize="15" Foreground="Gray" Margin="0,20,0,0" />
                <TextBox x:Name="txtDetail"  Width="300" RelativePanel.AlignVerticalCenterWith="lblDetail" RelativePanel.RightOf="lblDetail" FontSize="15" Margin="30,20,0,0" Height="32" VerticalAlignment="Top" BorderThickness="1" />
                <TextBlock x:Name="lblType"  Margin="0,20,0,0" Text="타입" FontSize="15" RelativePanel.AlignLeftWith="lblTitle" Foreground="Gray" RelativePanel.Below="lblDetail"/>
                <RadioButton x:Name="rbTypeCash" Content="현금"  FontSize="15" RelativePanel.RightOf="lblType" RelativePanel.AlignVerticalCenterWith="lblType" Margin="66,20,0,0"/>
                <RadioButton x:Name="rbTypeCard" Content="카드"  FontSize="15" RelativePanel.RightOf="rbTypeCash" RelativePanel.AlignVerticalCenterWith="rbTypeCash" Margin="0,20,0,0"/>
                <TextBlock x:Name="lblCost" Text="금액" FontSize="15" RelativePanel.AlignLeftWith="lblTitle" RelativePanel.Below="lblType" Margin="0,20,0,0" Foreground="Gray"/>
                <TextBox x:Name="txtCost" Text="" FontSize="15" InputScope="Number" RelativePanel.RightOf="lblCost" RelativePanel.AlignVerticalCenterWith="lblCost" Width="300" Margin="66,20,0,0" BorderThickness="1" />
                <Button x:Name="btnInputCost" Content="입력" FontSize="15" RelativePanel.AlignRightWithPanel="True" RelativePanel.Below="lblCost" Margin="-25,40,0,0" Background="#FF09B1CE" BorderBrush="#FF09B1CE" BorderThickness="0" Foreground="White" Click="btnInputCost_Click" Width="71" HorizontalAlignment="Right"/>
            </RelativePanel>
        </RelativePanel>
        <StackPanel Grid.Column="1" Orientation="Vertical">
            <TextBlock x:Name="lblHistoryTitle" Text="오늘 지출 내역" FontSize="20" FontWeight="Bold">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{ThemeResource SystemBaseHighColor}"/>
                </TextBlock.Foreground>
            </TextBlock>
            <Controls:ViewCostHistoryControl x:Name="costHistory" Margin="0,14,0,0"/>
        </StackPanel>
    </Grid>
</Page>
