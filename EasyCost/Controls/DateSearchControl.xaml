<UserControl
    x:Class="EasyCost.Controls.DateSearchControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyCost.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="370">

    <!--<Grid>
        <DatePicker HorizontalAlignment="Left" Margin="10,25,0,0" VerticalAlignment="Top"/>

    </Grid>-->
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="시작" Width="60" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <CalendarDatePicker Width="auto" HorizontalAlignment="Center"/>
            <TextBlock Text="시작" Width="60" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <CalendarDatePicker Width="auto" HorizontalAlignment="Center"/>
        </StackPanel>
        <RelativePanel Grid.Row="3">
            <Button Name="btnOK" Content="선택" RelativePanel.AlignRightWithPanel="True" Margin="0,5,10,5"/>
        </RelativePanel>
    </Grid>
</UserControl>
